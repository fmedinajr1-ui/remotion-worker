// remotion/src/compositions/ResultsRecap.tsx
// Honest recap template. Same structure as pick-reveal, different pacing.

import React from 'react';
import { VideoComposition, VideoCompositionProps } from './VideoComposition';

export const ResultsRecap: React.FC<VideoCompositionProps> = (props) => {
  return <VideoComposition {...props} />;
};
